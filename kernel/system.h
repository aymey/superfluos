#ifndef __SYSTEM_H_
#define __SYSTEM_H_

unsigned char *memset(unsigned char *dest, int ch, int count);
unsigned char *memcpy(unsigned char* dest, unsigned char* src, int count); // should i use void*?

#endif
